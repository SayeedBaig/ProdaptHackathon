# PitchPilot Frontend Environment Variables
# Copy to .env.local for local overrides. Do NOT commit .env or .env.local.

# Base API URL for PitchPilot backend
VITE_API_URL=http://localhost:8000

# Set to 'true' to run fully offline with simulated latency & fixtures
VITE_USE_MOCKS=true
